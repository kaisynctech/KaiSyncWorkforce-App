<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="KaiFlow.Timesheets.Views.Auth.EmployeeRegisterVerifyPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:KaiFlow.Timesheets.ViewModels.Auth"
    x:DataType="vm:EmployeeRegisterVerifyViewModel"
    BackgroundColor="{StaticResource BackgroundDark}"
    Title="Verify Email">

    <VerticalStackLayout Padding="32" Spacing="16">

        <Label Text="Verify your email"
               Style="{StaticResource HeadlineMedium}" />
        <Label Style="{StaticResource BodySmall}" TextColor="{StaticResource TextSecondary}">
            <Label.FormattedText>
                <FormattedString>
                    <Span Text="We sent a 6-digit code to " />
                    <Span Text="{Binding Email}" FontAttributes="Bold" />
                </FormattedString>
            </Label.FormattedText>
        </Label>

        <Entry Placeholder="Enter verification code" Text="{Binding Otp}"
               Keyboard="Numeric" Style="{StaticResource DarkEntry}"
               Margin="0,16,0,0" />

        <Label Text="{Binding ErrorMessage}" TextColor="{StaticResource Error}"
               IsVisible="{Binding ErrorMessage, Converter={StaticResource IsNotNullConverter}}" />

        <Button Text="Verify"
                Command="{Binding VerifyCommand}"
                IsEnabled="{Binding IsNotBusy}"
                Style="{StaticResource PrimaryButton}"
                Margin="0,8,0,0" />

        <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}"
                           Color="{StaticResource Primary}" HorizontalOptions="Center" />

        <Button Text="Resend code"
                Command="{Binding ResendCommand}"
                Style="{StaticResource TextButton}" FontSize="13" />
    </VerticalStackLayout>
</ContentPage>
